//---------------------------------------------------------------------------

#pragma hdrstop
#include <stdio.h>
//---------------------------------------------------------------------------

#pragma argsused
int main(int argc, char* argv[])
{
        int r = 0;
        scanf("%d",&r);
        if(r == 1)  //Benim yazdýðým
        {
                int i,enbuyuk = 0;
                int dizi[10];
                for(i = 0;i < 10;i++)
                {
                        printf("Lutfen %d. degeri giriniz : ",(i+1));
                        scanf("%d",&dizi[i]);
                        if(enbuyuk < dizi[i])
                        {
                                enbuyuk = dizi[i];
                        }
                }
                printf("En buyuk deger : %d",enbuyuk);
                getchar();
                getchar();
        }
        else   // Hocanýn Yazýðý
        {
                int i,enbuyuk = 0;
                int dizi[10];
                printf("Lutfen 10 adet sayi giriniz : ");

                for(i = 0;i < 10;i++)
                {
                        scanf("%d",&dizi[i]);
                }
                for(i = 0;i < 10;i++)
                {
                        if(enbuyuk < dizi[i])
                        {
                                enbuyuk = dizi[i];
                        }
                }
                printf("En buyuk sayi : %d",enbuyuk);
                getchar();
                getchar();
        }

        return 0;
}
//---------------------------------------------------------------------------
 